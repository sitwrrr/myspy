# MemOS 文件整理说明

## ✅ 整理完成

所有 MemOS 相关文件已归纳到 `memos_system/` 文件夹。

---

## 📁 新的文件组织

```
项目根目录/
├── memos_system/               ← 所有 MemOS 文件
│   ├── api/                    ← API 服务
│   ├── webui/                  ← Web 界面
│   ├── config/                 ← 配置文件
│   ├── data/                   ← 数据存储
│   ├── scripts/                ← 工具脚本
│   └── docs/                   ← 所有文档
├── MEMOS-API.bat               ← 启动脚本（已更新路径）
├── MEMOS-WebUI.bat             ← 启动脚本（已更新路径）
├── 迁移记忆.bat                ← 启动脚本（已更新路径）
└── MemOS-README.txt            ← 快速说明
```

---

## 🔄 已更新的路径

### API 服务
- **完整版**: `memos_system/api/memos_api_server_full.py`
- **简化版**: `memos_system/api/memos_api_server.py`

### 配置文件
- `memos_system/config/memos_config.json`

### 数据存储
- `memos_system/data/memory_store.json`

### RAG Model
- **保持不变**: `./RAG-model/` （项目根目录）
- API 服务会自动找到正确路径

---

## ✅ 启动脚本已自动更新

所有 `.bat` 脚本已更新路径，**无需修改**，直接使用：

```
双击: MEMOS-API.bat      → 自动找到 api/ 文件夹
双击: MEMOS-WebUI.bat    → 自动找到 webui/ 文件夹
双击: 迁移记忆.bat        → 自动找到 scripts/ 文件夹
```

---

## 🗑️ 可以删除的旧文件（如果存在）

根目录下可能残留的旧文件：
- `memos_data/` 文件夹（旧数据目录）
- 任何散落的 `MemOS*.md` 文件（已移到 docs/）

**这些都可以安全删除**，新数据在 `memos_system/data/`

---

## 📖 文档位置

所有文档现在在 `memos_system/docs/`：

- `README.md` - 快速参考
- `MemOS快速启动指南.md` - 详细入门
- `MemOS使用说明.md` - 完整说明
- `MemOS完整框架说明.md` - 框架文档
- `MemOS功能检查清单.md` - 测试清单
- `MemOS集成总结.md` - 技术总结

---

## 🎯 下一步

1. **查看主README**: `memos_system/README.md`
2. **启动完整版**: `MEMOS-API.bat` → 选择 1
3. **导入记忆**: `迁移记忆.bat`

---

*文件整理完成时间: 2025-12-20*

